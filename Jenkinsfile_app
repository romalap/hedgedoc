pipeline {
    agent { label 'node1_app' } 
    stages {
        stage('run_docker') {
            steps {
            sh '''
            echo "VERSION=${params.VERSION}" > .env
            sudo docker-compose up -d
            '''
            }
        }
    }
}