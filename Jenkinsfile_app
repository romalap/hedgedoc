pipeline {
    agent { label 'node2_web' } 
    stages {
        stage('run_docker') {
            steps {
            sh 'sudo docker-compose -f docker-compose-app.yaml up -d'
            }
        }
    }
}