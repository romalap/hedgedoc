pipeline {
    agent { label 'node1_sql' 
    node { label 'node1_sql' }
    }
    stages {
        stage('run_docker') {
            steps {
            sh 'docker create mariadb:10'
            }
        }
    }
}