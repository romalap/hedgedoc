pipeline {
 //   agent none
    agent {
    node {label 'node1_sql'}
    }
    stages {
        stage('Test curl') {
            agent {
                docker { 
                    image 'mariadb:10' 
                    args "--network=host"
                }
            }
            steps {
                sh """#!/bin/sh

                """
            }
        }
    }
}